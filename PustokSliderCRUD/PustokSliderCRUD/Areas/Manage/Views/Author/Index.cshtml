@model List<Author>
@{
    var counter = 0;
}
<div class="container-fluid justify-content-between align-items-center row">
    <h1>Author Page</h1>
    <a asp-action="create" asp-controller="author" class="btn btn-primary">Create</a>

    <div class="container-fluid">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">FullName</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var author in Model)
                {
                    counter++;
                    <tr>
                        <th scope="row">@counter</th>
                        <td>@author.Fullname</td>
                        <td>
                            <a asp-action="update" asp-controller="author" asp-route-id="@author.Id" class="btn btn-success">Update</a>
                            <a asp-action="delete" asp-controller="author" asp-route-id="@author.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

