@model List<Tag>
@{
    var counter = 0;
}
<div class="container-fluid justify-content-between align-items-center row">
    <h1>Tag Page</h1>
    <a asp-action="create" asp-controller="tag" class="btn btn-primary">Create</a>

    <div class="container-fluid">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">Name</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tag in Model)
                {
                    counter++;
                    <tr>
                        <th scope="row">@counter</th>
                        <td>@tag.Name</td>
                        <td>
                            <a asp-action="update" asp-controller="tag" asp-route-id="@tag.Id" class="btn btn-success">Update</a>
                            <a asp-action="delete" asp-controller="tag" asp-route-id="@tag.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

